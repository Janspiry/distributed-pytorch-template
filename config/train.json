{
    "name": "base_model",
    "phase": "train", // train/test/debug
    "gpu_ids": [ // gpu ids list, default is 0
        0 
    ],
    "finetune_norm": false,
    "path": { //set the file path
        "log": "logs", // base root path
        "tb_logger": "tb_logger", // tensorboard logger
        "results": "results",
        "checkpoint": "checkpoint",
        "resume_state": null
    },
    "datasets": {
        "train": {
            "name": "image_dataset", // use which dataset, indicates dataset's file name
            "dataroot": "/home/huangyecheng/dataset/cmfd/comofod",
            "batch_size": 2,
            "num_workers": 8,
            "use_shuffle": true,
            "pin_memory": true,
            "data_len": -1 // -1 represents all data used in training
        },
        "val": {
            "name": "image_dataset",
            "dataroot": "/home/huangyecheng/dataset/cmfd/comofod_jlw_ae",
            "batch_size": 1,
            "num_workers": 4,
            "pin_memory": true,
            "data_len": 50
        },
        "test": { // to do 
            
        }
    },
    "model": {
        "which_model": "ae", // use which trainer, indicates trainer's file name in models path
        "which_module": "ae" // use which model structure, indicates structure's file name in models/modules path
    },
    "training": {
        "n_iter": 1000000, // max iterations
        "n_epoch": 1e5, // max epochs, not limited now
        
        "val_freq": 1e4,
        "save_checkpoint_freq": 1e4,
        "display_freq": 1e3,
        "print_freq": 50
    }
}